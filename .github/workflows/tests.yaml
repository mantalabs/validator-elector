name: Tests

on: 
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    name: go build
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v1
      - run: go mod download
      - run: go build
